double get_beam_height ( double hrap_coord_x,
                         double hrap_coord_y,
                         double hrap_x,
                         double hrap_y )
{
   double height;

   /* If the file has not been read, read the file. */
   /* Return the height corresponding to the hrap coordinates
      supplied by the user */
   
   return height;
}
