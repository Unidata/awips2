#
# AWIPS II Python pmw Site-Package Spec File
#
Name: awips2-python-pmw
Summary: AWIPS II Python pmw Site-Package
Version: 1.3.2
Release: 2
Group: AWIPSII
BuildRoot: /tmp
URL: N/A
License: N/A
Distribution: N/A
Vendor: Raytheon
Packager: Bryan Kowal

AutoReq: no
requires: awips2-python
provides: awips2-python-pmw
%define _docdir python.pmw

%description
AWIPS II Python pmw Site-Package - Installs the AWIPS II Python
pmw Site-Package in the Python installation.

# Turn off the brp-python-bytecompile script
%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')

%prep
# Verify That The User Has Specified A BuildRoot.
if [ "${RPM_BUILD_ROOT}" = "/tmp" ]
then
   echo "An Actual BuildRoot Must Be Specified. Use The --buildroot Parameter."
   echo "Unable To Continue ... Terminating"
   exit 1
fi

mkdir -p ${RPM_BUILD_ROOT}
mkdir -p ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build

%build
SITE_PACKAGE_TAR_DIR="pythonPackages/pmw"
SITE_PACKAGE_SRC="Pmw.1.3.2.tar.gz"
SITE_PACKAGE_SRC_DIR="Pmw.1.3.2"

cp ${WORKSPACE_DIR}/${SITE_PACKAGE_TAR_DIR}/${SITE_PACKAGE_SRC} \
   ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build
   
cd ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build
tar -xvf ${SITE_PACKAGE_SRC}
cd ${SITE_PACKAGE_SRC_DIR}/src
${PYTHON_EXE} setup.py build
RC="$?"
if [ ! "${RC}" = 0 ]; then
   exit 1
fi

%install
SITE_PACKAGE_SRC_DIR="Pmw.1.3.2"

cd ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build/${SITE_PACKAGE_SRC_DIR}/src

${PYTHON_EXE} setup.py install --root=${RPM_BUILD_ROOT} \
   --prefix=/awips2/python
   
if [ -d ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build ]; then
   rm -rf ${RPM_BUILD_ROOT}/tmp/awips2-python/python-build
fi

%pre
if [ -d /usr/share/doc/awips2/%{_docdir} ]; then
   rm -rf /usr/share/doc/awips2/%{_docdir}
fi

PYTHON_INSTALL="/awips2/python"
echo -e "\e[1;34m--------------------------------------------------------------------------------\e[m"
echo -e "\e[1;34m\| Installing the AWIPS II Python pmw Site-Package...\e[m"
echo -e "\e[1;34m--------------------------------------------------------------------------------\e[m"
echo -e "\e[1;34m   Python Install Root = ${PYTHON_INSTALL}\e[m"

%post
echo -e "\e[1;32m--------------------------------------------------------------------------------\e[m"
echo -e "\e[1;32m\| AWIPS II Python pmw Site-Package Installation - COMPLETE\e[m"
echo -e "\e[1;32m--------------------------------------------------------------------------------\e[m"

%preun

%postun
echo -e "\e[1;34m--------------------------------------------------------------------------------\e[m"
echo -e "\e[1;34m\| The AWIPS II Python pmw Site-Package Has Been Successfully Removed\e[m"
echo -e "\e[1;34m--------------------------------------------------------------------------------\e[m"
echo ""

%files
%defattr(644,awips,fxalpha,755)
/awips2/python/lib/python2.7/site-packages/*