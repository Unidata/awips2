%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%define _build_arch %(uname -i)
%define _python_pkgs_dir "%{_baseline_workspace}/pythonPackages"

#
# AWIPS II Python shapely Spec File
#

Name: awips2-python-shapely
Summary: AWIPS II Python shapely Distribution
Version: 1.2.16
Release: 1
Group: AWIPSII
BuildRoot: %{_build_root}
BuildArch: %{_build_arch}
URL: N/A
License: N/A
Distribution: N/A
Vendor: Raytheon
Packager: Bryan Kowal

AutoReq: no
requires: awips2-python
provides: awips2-python-shapely

%description
AWIPS II Python shapely Site-Package

%prep
# Verify That The User Has Specified A BuildRoot.
if [ "%{_build_root}" = "" ]
then
   echo "A Build Root has not been specified."
   echo "Unable To Continue ... Terminating"
   exit 1
fi

if [ -d ${RPM_BUILD_ROOT} ]; then
   rm -rf ${RPM_BUILD_ROOT}
   if [ $? -ne 0 ]; then
      exit 1
   fi
fi

rm -rf %{_build_root}
mkdir -p %{_build_root}
mkdir -p %{_build_root}/build-python

PYTHON_RPM_DIR="%{_baseline_workspace}/rpms/python.site-packages"
INSTALL_SETUPTOOLS_SH="${PYTHON_RPM_DIR}/deploy.builder/install-setuptools.sh"
# install setuptools
/bin/bash ${INSTALL_SETUPTOOLS_SH} %{_baseline_workspace} \
   %{_python_pkgs_dir} %{_build_root}/build-python
if [ $? -ne 0 ]; then
   exit 1
fi

rm -rf %{_build_root}/build-python
mkdir -p %{_build_root}/build-python
mkdir -p %{_build_root}/awips2/python/lib
mkdir -p %{_build_root}/awips2/python/bin
mkdir -p %{_build_root}/awips2/python/include

%build
# build geos
__GEOS_TAR=geos-3.3.6.tar.bz2
__GEOS_UNTARRED=geos-3.3.6
PYTHON_RPM_DIR="%{_baseline_workspace}/rpms/python.site-packages"
INSTALLER_SHAPELY_DIR="${PYTHON_RPM_DIR}/Installer.shapely"

cp ${INSTALLER_SHAPELY_DIR}/${__GEOS_TAR} \
   %{_build_root}/build-python
if [ $? -ne 0 ]; then
   exit 1
fi

cd %{_build_root}/build-python
tar -xvf ${__GEOS_TAR}
if [ $? -ne 0 ]; then
   exit 1
fi
cd ${__GEOS_UNTARRED}
./configure --prefix=%{_build_root}/awips2/python
if [ $? -ne 0 ]; then
   exit 1
fi
make
if [ $? -ne 0 ]; then
   exit 1
fi
make install
if [ $? -ne 0 ]; then
   exit 1
fi

# rename libgeos and libgeos_c to prevent conflicts
# with the version of geos that we are using for PostgreSQL.
# if we finally update PostgreSQL, we will be able to use
# the same geos libraries for PostgreSQL and Python Shapely

# this will ensure that python shapely will be bound to the newer
# version of geos because it is not compatible with the version
# of geos that we are currently using for PostgreSQL

__GEOS_C_SO=libgeos_c.so
__GEOS_C_336_SO=libgeos_c336.so
__GEOS_SO=libgeos.so
__GEOS_336_SO=libgeos336.so
__GEOS_A=libgeos.a
__GEOS_336_A=
__GEOS_C_A=libgeos_c.a
__GEOS_C_336_A=

pushd . > /dev/null 2>&1
cd %{_build_root}/awips2/python/lib
mv ${__GEOS_C_SO} ${__GEOS_C_336_SO}
if [ $? -ne 0 ]; then
   exit 1
fi
mv ${__GEOS_SO} ${__GEOS_336_SO}
if [ $? -ne 0 ]; then
   exit 1
fi
popd > /dev/null 2>&1

# build the shapely python site-package
SHAPELY_SRC_DIR="%{_python_pkgs_dir}/shapely"
__SHAPELY=shapely
__SHAPELY_TAR=Shapely-1.2.16.tar.gz
__SHAPELY_UNTARRED=Shapely-1.2.16

cp %{_python_pkgs_dir}/${__SHAPELY}/${__SHAPELY_TAR} \
   %{_build_root}/build-python
if [ $? -ne 0 ]; then
   exit 1
fi
cd %{_build_root}/build-python

tar -xvf ${__SHAPELY_TAR}
if [ $? -ne 0 ]; then
   exit 1
fi
cd ${__SHAPELY_UNTARRED}
export CPPFLAGS="-I%{_build_root}/awips2/python/include"
export CPPFLAGS="${CPPFLAGS} -L%{_build_root}/awips2/python/lib"
/awips2/python/bin/python setup.py build
if [ $? -ne 0 ]; then
   exit 1
fi
/awips2/python/bin/python setup.py install \
   --root=%{_build_root} \
   --prefix=/awips2/python
if [ $? -ne 0 ]; then
   exit 1
fi

rm -rf %{_build_root}/build-python
if [ $? -ne 0 ]; then
   exit 1
fi
exit 1

%clean
rm -rf %{_build_root}

%files
%defattr(644,awips,fxalpha,755)
%dir /awips2/python/bin
/awips2/python/bin/*
%dir /awips2/python/lib
/awips2/python/lib/*

exit 1

%install